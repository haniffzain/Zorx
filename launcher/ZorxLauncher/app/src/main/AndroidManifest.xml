<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<uses-permission android:name="android.permission.START_TASKS_FROM_RECENTS" />
<uses-permission android:name="android.permission.INTERNAL_SYSTEM_WINDOW" />
<uses-permission android:name="android.permission.MANAGE_ACTIVITY_TASKS" />
<uses-permission android:name="android.permission.REMOVE_TASKS" />
<uses-permission android:name="android.permission.REAL_GET_TASKS" />

    <application
        android:theme="@style/Theme.ZorxLauncher"
        android:label="Zorx Launcher"
        android:icon="@drawable/zorx_logo"
        android:allowBackup="true"
        android:supportsRtl="true">

<activity
    android:name=".desktop.DesktopActivity"
    android:theme="@style/Theme.ZorxLauncher"
    android:screenOrientation="landscape"
    android:exported="false" />

<activity
    android:name=".recents.RecentsActivity"
    android:theme="@style/Theme.ZorxLauncher"
    android:exported="true"
    android:excludeFromRecents="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Menjadikan Zorx Launcher sebagai Home Launcher -->
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.HOME" />

                <category android:name="android.intent.category.DEFAULT" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>
